# Server
PORT=8080

# Redis
REDIS_URL=redis://redis:6379

# Paths inside containers
MUSIC_DIR=/data/music
STAGING_DIR=/data/staging

# Optional: Navidrome triggers (not wired by default)
NAVIDROME_SCAN_URL=http://navidrome:4533/api/rescan
NAVIDROME_TOKEN=
